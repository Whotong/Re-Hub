-- Key = game.GameId (number). Value = repo-relative script path.
-- Paths are prepended with BASE_URL by main.lua and fetched via loadstring(HttpGet).
return {
	[10563114921] = "Supported-Games/Steal%20an%20Egg.lua",
}
